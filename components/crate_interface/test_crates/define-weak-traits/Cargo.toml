[package]
name = "define-weak-traits"
version = "0.3.1"
edition = "2021"
publish = false
license = "Apache-2.0"

[dependencies]
# Don't use workspace dependency here because we want to test the weak_default feature
ax-crate-interface = { path = "../..", features = ["weak_default"] }
