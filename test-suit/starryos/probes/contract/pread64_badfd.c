/* Generated by materialize_syscall_batch.py */
#include <errno.h>
#include <stdio.h>
#include <unistd.h>

int main(void)
{
	errno = 0;
	char b; ssize_t r = pread(-1, &b, 1, 0);
	int e = errno;
	dprintf(1, "CASE pread64.badfd ret=%ld errno=%d note=handwritten\n", (long)r, e);
	return 0;
}

