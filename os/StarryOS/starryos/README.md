# starryos

`starryos` is the StarryOS kernel binary crate built on top of ArceOS.

This packaged crate contains the StarryOS entry point and startup script used to
boot the kernel. For project-wide setup, supported architectures, and build or
run guidance, see the StarryOS repository documentation.
